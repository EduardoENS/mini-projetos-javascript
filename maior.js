// Escreva uma função que usa 2 números e retorna o maior entre eles

let maiorValor = maior(5,6);
    console.log(maiorValor);

function maior(valor1, valor2) {
    return valor1 > valor2 ? valor1 : valor2;
}